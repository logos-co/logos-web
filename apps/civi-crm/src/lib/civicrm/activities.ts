// TODO: Activity queries + create
// Functions to implement:
//   listActivities(caseId: string, page: number): Promise<{ items: ActivityItem[]; total: number }>
//   createActivity(params: { caseId?: string; targetContactId?: string; sourceContactId: string; subject: string }): Promise<void>
// Note: activity_type_id for CMS-created entries is 80
