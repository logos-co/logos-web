// TODO: Case-entity queries — list (paginated + filtered), get-by-id, update
// Functions to implement:
//   listCases(params: { page: number; assigneeContactId?: string; status?: string; sort?: string; order?: 'asc' | 'desc' }): Promise<{ items: CaseListItem[]; total: number }>
//   getCaseById(id: string): Promise<CaseDetail>
//   updateCase(id: string, values: Record<string, unknown>): Promise<void>
