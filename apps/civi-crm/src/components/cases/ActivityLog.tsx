'use client'

// TODO: read-only paginated activity log — uses Pagination primitive from @acid-info/logos-ui
// Fetches from GET /api/cases/[id]/activities?page= on page change
export default function ActivityLog() {
  return null
}
