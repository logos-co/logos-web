'use client'

// TODO: editable Case fields form — sections and fields driven by active view config's detailSections.case
// Calls updateCase() Server Action on submit
export default function CaseFields() {
  return null
}
