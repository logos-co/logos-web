/**
 * Shared copy shape for the Broadcast Network page sections. Each section file
 * imports this and renders the slice it needs.
 */
export interface BroadcastNetworkCopy {
  title: string
  introPrimary: string
  introSecondary: string
  eventFallbackDescription: string
  eventHost: string
  eventNextShow: string
  upcomingEvents: string
  calendarYearLabel: string
  calendarMonthLabel: string
  calendarTodayLabel: string
  pastEpisodes: string
  listenOnApp: string
}
