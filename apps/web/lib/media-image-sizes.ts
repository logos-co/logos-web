/**
 * `sizes` for images in the 700px media column. Kept apart from
 * lib/media-images so client components can import it without pulling in the
 * CMS data layer.
 */
export const MEDIA_BODY_IMAGE_SIZES =
  '(max-width: 767px) calc(100vw - 32px), 700px'
